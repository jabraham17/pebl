add_executable(test test.pebl)
# target_include_directories(test PRIVATE "${PROJECT_SOURCE_DIR}/include"
#                                          "${SOURCE_DIR}")
# target_link_libraries(test PRIVATE core)
# install(TARGETS test DESTINATION bin)
