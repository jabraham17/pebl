variables:
  COMPILER: ${BIN_DIR}/pebl
  OUTFILE: ${FILE}.out${EXT}
  COMP_CMD: ${COMPILER} -o ${OUTFILE}
  EXEC_CMD: ./${OUTFILE}
  CLEAN_CMD: rm ${OUTFILE}
tests:
- file: a.pebl
  configs:
  - cmds:
    - ${COMP_CMD} ${FILE} b.pebl
    - ${EXEC_CMD}
    - ${CLEAN_CMD}
